---
title: Xpert 숙제 - Web 기초
date: 2026-07-29 00:00:00 +0900
categories: [Xpert, 숙제]
tags: [web]
---

## Web 기본

### 관련 용어 정리

웹 서버(Web Server) : 정보를 제공하는 주체

웹 클라이언트(Web Client) : 정보를 받는 고객

프론트엔드(Front-end) : 클라이언트에게 요청을 받는 부분

백엔드(Back-end) : 클라이언트의 요청을 처리하는 부분

웹 리소스(Web Resource) : 페이지가 보여주고 있는 정보(글, 폰트, 배경, 이미지 등등), 모든 웹 리소스는 고유의 URL을 가지고 있다.
`https://dreamhack.io/index.html` → `dreamhack.io`에 있는 `index.html`을 가져와라는 뜻

![](/assets/img/Xpert(4)/01.png)

HTML(HyperText Markup Language) : 태그와 속성을 통한 구조화된 문서 작성을 지원

CSS(Cascading Style Sheets) : 웹 리소스들을 시각화 방법을 기재한 스타일 시트

JS(JavaScript) : 웹 문서의 동작을 정의

### 웹 클라이언트, 서버 사이의 통신

1. 클라이언트 접속, 서버에 HTTP 형식 리소스를 요청한다.
2. 서버에서 처리 이후 HTTP 리소스를 전달한다.
3. 클라이언트의 브라우저에서 서버에게 응답 받은 웹 리소스를 보여준다.

## HTTP(Hyper Text Transfer Protocol)

클라이언트의 Request와 서버의 Response 형식으로 정의한 프로토콜(HTTP 80번, HTTPS 443번 사용)

### HTTP의 구조

![](/assets/img/Xpert(4)/02.png)

#### HTTP Header

각 줄은 CRLF( 줄 바꿈을 나타내는 제어 문자열 )로 구분된다.
필드와 값으로 구성되며 HTTP의 메시지 속성, HTTP 바디의 속성을 나타낸다.

#### HTTP Body

빈 줄 뒤에 나타나며 클라이언트나 서버에 전송하는 데이터를 담고 있다.

#### HTTP 요청

서버에게 특정 동작을 요구하는 메시지

1. HTTP 요청의 시작 줄은 Method, Target, ver로 구성된다.
2. HTTP 표준에서 정의하는 메소드는 8개가 있다. (ex. GET, POST 등등)

GET : 리소스를 가져와달라고 요청하는 메소드

POST : 요청 대상에게 데이터를 보내는 메소드, 데이터는 바디에 저장돼서 보내진다.

#### HTTP 응답

요청에 대한 결과를 반환하는 메시지

1. HTTP 응답은 HTTP ver, Status Code, Reason Phrase(처리 사유)로 구성된다.
2. 상태코드는 요청에 대한 처리 결과를 나타낸다. HTTP 표준인 RFC 2616은 대략 40여개의 상태 코드를 정의하고 있다.

| 상태 코드 | 설명 | 대표 예시 |
| --- | --- | --- |
| 1xx | 요청을 제대로 받았고, 처리가 진행 중임 | |
| 2xx | 요청이 제대로 처리됨 | • 200(OK): 성공 |
| 3xx | 요청을 처리하려면, 클라이언트가 추가 동작을 취해야 함 | • 302(Found): 다른 URL로 갈 것 |
| 4xx | 클라이언트가 잘못된 요청을 보내어 처리에 실패했다 | • 400(Bad Request): 요청이 문법에 맞지 않음<br>• 401(Unauthorized): 클라이언트가 요청한 리소스에 대한 인증이 실패함<br>• 403(Forbidden): 클라이언트가 리소스에 요청할 권한이 없음<br>• 404(Not Found): 리소스가 없음 |
| 5xx | 클라이언트의 요청은 유효하지만, 서버에 에러가 발생하여 처리에 실패했다 | • 500(Internal Server Error): 서버가 요청을 처리하다가 에러가 발생함<br>• 503(Service Unavailable): 서버가 과부하로 인해 요청을 처리할 수 없음 |

### HTTPS

HTTP에서 SSL과 TLS 프로토콜을 도입하여 이런 문제점을 보완한다.

![](/assets/img/Xpert(4)/03.png)

TLS는 기밀성, 무결성, 인증을 보장해준다.

## Web Browser

웹 브라우저는 서버와의 통신을 대신해주고 수신한 리소스를 시각화 해주는 클라이언트 도구

### URL(Uniform Resource Locator)

웹에 있는 리소스의 위치를 표현해주는 문자열

![](/assets/img/Xpert(4)/04.png)

| 요소 | 설명 |
| --- | --- |
| Scheme | 웹 서버와 어떤 프로토콜로 통신할지 나타낸다. |
| Host | Authority의 일부로, 접속할 웹 서버의 주소에 대한 정보를 가지고 있다. |
| Port | Authority의 일부로, 접속할 웹 서버의 포트에 대한 정보를 가지고 있다. |
| Path | 접근할 웹 서버의 리소스 경로로 `/`로 구분된다. |
| Query | 웹 서버에 전달하는 파라미터이며 URL에서 `?` 뒤에 위치한다. |
| Fragment | 메인 리소스에 존재하는 서브 리소스를 접근할 때 이를 식별하기 위한 정보를 담고 있다. `#` 문자 뒤에 위치한다. |

### Domain Name

Host는 Domain Name과 IP Address의 값을 가질 수 있다. IP 주소는 불규칙한 값이라 사람이 외우기 어려워 이를 Domain Name이 이름으로 치환해서 IP대신 사용한다.

DNS(Domain Name Server)는 Domain Name에 대해 질의하고 정보를 얻을 수 있는 서버이다.

`nslookup` 명령어를 이용해서 확인할 수 있다.

```bash
$ nslookup example.com
Server:		8.8.8.8
Address:	8.8.8.8#53

Non-authoritative answer:
Name:	example.com
Address: 93.184.216.34
```

### Web Rendering

서버로부터 받은 리소스를 사용자에게 시각화하는 행위를 말한다.

ex) Webkit, Blink, Gecko 등이 있다.

## DevTools (개발자 도구)

개발자 도구는 웹 서비스를 진단할 수 있는 도구이다. 유용한 도구인 만큼 웹 취약점을 이용하려는 공격자에게도 유용하게 사용될 수 있다.

![](/assets/img/Xpert(4)/05.png)

| 색상 | 설명 |
| --- | --- |
| 🔴 | 요소 검사(Inspect) 및 디바이스 툴바(Device Toolbar) |
| 🟠 | 기능을 선택하는 패널 |
| 🟡 | 현재 페이지에서 발생한 에러 및 경고 메시지 |
| 🟢 | 개발자 도구 설정 |

### 요소검사

![](/assets/img/Xpert(4)/06.png)

특정 요소의 개괄적인 정보를 파악하고, 이와 관련된 코드를 쉽게 찾을 수 있다.

### 디바이스 툴바

![](/assets/img/Xpert(4)/07.png)

현재 브라우저의 화면 비율 및 User-Agent를 원하는 값으로 변경할 수 있다.

### Elements

![](/assets/img/Xpert(4)/08.png)

현재 페이지의 HTML코드를 볼 수 있고 보여지는 HTML코드를 수정할 수 있다.

### Console

![](/assets/img/Xpert(4)/09.png)

콘솔은 프론트엔드의 JS 코드에서 발생한 각종 메시지를 출력, 이용자가 입력한 JS 코드를 실행해주는 도구이다.

JS 웹 개발을 할 때 console 오브젝트에 개발자 도구의 콘솔에 접근할 수 있는 함수들이 정의되어있다.
코드를 작성하면서 어떤 변수의 중간 값을 출력해보고 싶다면 `console.log` 를 이용할 수 있다.

```javascript
> console
Console {
  log: [Function: bound consoleCall],
  debug: [Function: bound consoleCall],
  info: [Function: bound consoleCall],
  dirxml: [Function: bound consoleCall],
  warn: [Function: bound consoleCall],
  error: [Function: bound consoleCall],
  ...
  context: [Function: context],
  [Symbol(counts)]: Map {},
  [Symbol(kColorMode)]: 'auto' }
```

### Sources

![](/assets/img/Xpert(4)/10.png)

현재 페이지를 구성하는 웹 리소스들을 확인 할 수 있다.

→ Elements와 차이점은 Sources는 서버가 실제로 전송한 원본 파일을 확인할 수 있다.

| 색상 | 설명 |
| --- | --- |
| 🔴 | 현재 페이지의 리소스 파일 트리, 파일 시스템 |
| 🟠 | 선택한 리소스 상세 보기 |
| 🟢 | 디버깅 정보<br>• **Watch**: 원하는 자바스크립트 식을 입력하면, 코드 실행 과정에서 해당 식의 값 변화를 확인할 수 있다.<br>• **Call Stack**: 함수들의 호출 순서를 스택 형태로 보여준다. 예를 들어, `A → B → C` 순서로 함수가 호출되어 현재 `C` 내부의 코드를 실행하고 있다면, Call Stack의 가장 위에는 `C`, 가장 아래에는 `A`가 위치한다.<br>• **Scope**: 정의된 모든 변수들의 값을 확인할 수 있다.<br>• **Breakpoints**: 브레이크포인트들을 확인하고, 각각을 활성화 또는 비활성화할 수 있다. |

### Network

![](/assets/img/Xpert(4)/11.png)

서버와 오가는 데이터를 확인할 수 있다.

| 색상 | 설명 |
| --- | --- |
| 🔴 | 로깅 중지 및 로그 전체 삭제 |
| 🟠 | 로그 필터링 및 검색 |
| 🟢 | 옵션<br>• Preserve log: 새로운 페이지로 이동해도 로그를 삭제하지 않는다.<br>• Disable cache: 이미 캐시된 리소스도 서버에 요청한다. |
| 🟡 | 네트워크 로그 |
| 🔵 | 네트워크 로그 요약 정보 |

내용을 Copy해서 `Console` 패널에 붙여서 실행시키면 동일한 요청을 재전송 할 수 있다.

### Application

쿠키, 캐쉬, 이미지, 폰트, 스타일시트 등 웹 애플리케이션과 관련된 리소스를 조회할 수 있다.

![](/assets/img/Xpert(4)/12.png)

### 추가적인 기능

`Ctrl + U` = 페이지의 소스를 볼 수 있다.

Secret browsing mode : 쿠키, 사이트 데이터, 방문기록, 권한 등등이 저장되지 않는 사이트를 만들어준다.

## 워 게임 풀이

### Devtools-sources

개발자 도구를 이용해서 주어진 파일에서 Flag를 찾으면 된다.

![](/assets/img/Xpert(4)/13.png)

검색을 통해 main.scss에 Flag가 들어있는 것을 확인할 수 있다.
